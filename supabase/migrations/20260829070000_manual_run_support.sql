-- =====================================================================
-- 수동 실행(웹 화면의 "지금 분석하기" 버튼) 지원
-- =====================================================================
-- 화면에서 "분석 대기 중인 기사 N건" 을 보여주려면 익명 사용자도
-- 미분류 기사 수를 셀 수 있어야 한다. 제목과 링크만 있는 공개 정보다.
grant select on public.unclustered_articles to anon, authenticated;
